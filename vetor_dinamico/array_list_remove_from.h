#ifndef __ARRAY__LIST__REMOVE__FROM__
#define __ARRAY__LIST__REMOVE__FROM__
#include "array_list_int.h"
/*
  Remover um elemento do array no indice especificado
    *list = ponteiro para o array.
    index = indice que indica onde será removido o inteiro.
*/

int array_list_remove_from(struct array_list_int* list, int index);

#endif
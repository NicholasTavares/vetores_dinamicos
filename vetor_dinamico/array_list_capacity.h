#ifndef __ARRAY__LIST__CAPACITY__
#define __ARRAY__LIST__CAPACITY__
#include "array_list_int.h"

/*
  Retorna o tamanho da capacidade do array, isto é, quando inteiros pode
  armazenar.
  *list = ponteiro para o array.
*/

unsigned int array_list_capacity(struct array_list_int* list);

#endif